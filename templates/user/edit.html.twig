{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-dark" href="{{ path('user_index') }}">Back</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
